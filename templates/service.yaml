apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise-node-app.fullname" . }}
  labels:
    {{- include "enterprise-node-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enterprise-node-app.selectorLabels" . | nindent 4 }}
